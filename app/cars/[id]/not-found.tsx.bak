import Link from 'next/link';
import { Car, Search, Home } from 'lucide-react';

export default function CarNotFound() {
    return (
        <div className="min-h-[70vh] flex items-center justify-center bg-primary">
            <div className="container-swiss max-w-2xl text-center">
                <div className="bg-surface rounded-2xl p-12 shadow-xl border border-medium">
                    <div className="w-24 h-24 bg-orange-500/10 rounded-full flex items-center justify-center mx-auto mb-6">
                        <Car size={48} className="text-orange-500" />
                    </div>

                    <h1 className="text-4xl font-bold text-primary mb-4">
                        Makina nuk u gjet
                    </h1>

                    <p className="text-lg text-secondary mb-8">
                        Makina që po kërkon nuk ekziston ose është shitur.
                    </p>

                    <div className="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-lg mx-auto">
                        <Link
                            href="/cars"
                            className="flex items-center justify-center gap-2 p-4 bg-surface-2 hover:bg-orange-500 hover:text-white rounded-xl border border-medium transition-all group"
                        >
                            <Search size={20} />
                            <span>Shfleto makina të tjera</span>
                        </Link>

                        <Link
                            href="/"
                            className="flex items-center justify-center gap-2 p-4 bg-surface-2 hover:bg-orange-500 hover:text-white rounded-xl border border-medium transition-all group"
                        >
                            <Home size={20} />
                            <span>Kthehu në fillim</span>
                        </Link>
                    </div>

                    <div className="mt-8 pt-8 border-t border-medium">
                        <p className="text-sm text-muted">
                            Nëse mendoni se ky është një gabim, na kontaktoni për ndihmë.
                        </p>
                        <Link
                            href="/contact"
                            className="inline-block mt-2 text-orange-500 hover:underline text-sm"
                        >
                            Kontakto support →
                        </Link>
                    </div>
                </div>
            </div>
        </div>
    );
}